@layout('layouts.admin')

@section('content')

      <div class="container">
      <div class="row justify-content-center" >
        <div class="col-12 col-md-5 col-xl-12" style="padding:0px;">

          <div class="table mb-0">
            <table class="table table-sm table-nowrap card-table">
              <thead><tr><th>User Name</th><th>Options</th></tr></thead>
              <tbody>
                @each(user in users)
                <tr>
                  <td>{{ user.email }}</td>
                <td><a href="/u/{{user.username}}">View</a></td>
                  <td><a href="/admin/users/edit/{{ user._id }}">Edit</a></td>
                  <td><a href="/admin/users/delete/{{ user._id }}">Delete</a></td>
                </tr>
                @endeach
              </tbody>
            </table>
          </div>




                  </div>
                </div> <!-- / .row -->
              </div>

          @endsection
